{
  "name": "therapist-matcher",
  "version": "1.0.0",
  "description": "New client therapist matcher application for the Frog Point Therapy website",
  "main": "src/therapistMatcher.js",
  "scripts": {
    "test": "jest",
    "package": "cd src && rm -rf ../artifacts && mkdir -p ../artifacts && zip -r ../artifacts/lambda.zip .",
    "deploy": "./deploy.sh"
  },
  "author": "Eric Culley",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.911.0"
  }
}
